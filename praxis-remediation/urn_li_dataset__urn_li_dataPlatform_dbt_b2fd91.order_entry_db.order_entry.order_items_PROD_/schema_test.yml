# Generated by PRAXIS Trust Assessment
# Asset: order_items
# URN: urn:li:dataset:(urn:li:dataPlatform:dbt,b2fd91.order_entry_db.order_entry.order_items,PROD)
# Trust Score: B- (70.4/100)
# Generated: 2026-08-08T13:35:49.953612+00:00
#
# Grounded in DataHub schema metadata for dbt.order_items

version: 2

models:
  - name: order_items
    description: >
      Contains line items for each order
    columns:
      - name: order_id
        description: "Foreign key to the orders table"
        tests:
          - not_null
      - name: line_item_id
        description: "Line item sequence number within the order"
        tests:
          - not_null
      - name: product_id
        description: "Foreign key to the products table"
        tests:
          - not_null
      - name: unit_price
        description: "Selling price per unit at time of order"
        tests:
          - not_null
      - name: quantity
        description: "Number of units ordered"
        tests:
          - not_null
      - name: dispatch_date
        description: "Date when item was dispatched from warehouse"
        tests:
          - not_null
      - name: return_date
        description: "Date when item was returned if applicable"
        tests:
          - not_null
      - name: gift_wrap
        description: "Flag or type of gift wrapping requested"
        tests:
          - not_null
      - name: condition
        description: "Condition of product (e.g., New, Refurbished)"
        tests:
          - not_null
      - name: supplier_id
        description: "ID of the supplier if dropshipped"
        tests:
          - not_null
      - name: estimated_delivery
        description: "Estimated delivery date for this item"
        tests:
          - not_null
    tests:
      - dbt_utils.recency:
          datepart: day
          field: dispatch_date
          interval: 2