# Generated by PRAXIS Trust Assessment
# Asset: Essential KPI Measures
# URN: urn:li:dataset:(urn:li:dataPlatform:powerbi,b2fd91.datahub_order_entries.Essential_KPI_Measures,PROD)
# Trust Score: C- (56.7/100)
# Generated: 2026-08-08T13:47:30.300304+00:00
#
# Grounded in DataHub schema metadata for powerbi.Essential KPI Measures

version: 2

models:
  - name: essential kpi measures
    description: >
      
    columns:
      - name: Value
        description: "None"
        tests:
          - not_null
      - name: Total Revenue
        description: "SUMX('ORDER_DETAILS', 'ORDER_DETAILS'[order_total])"
        tests:
          - not_null
      - name: Total Orders
        description: "DISTINCTCOUNT('ORDER_DETAILS'[order_id])"
        tests:
          - not_null
      - name: Average Order Value
        description: "DIVIDE([Total Revenue], [Total Orders], 0)"
        tests:
          - not_null
      - name: Total Customers
        description: "DISTINCTCOUNT('ORDER_DETAILS'[customer_id])"
        tests:
          - not_null
      - name: Revenue Per Customer
        description: "DIVIDE([Total Revenue], [Total Customers], 0)"
        tests:
          - not_null
      - name: Total Products Sold
        description: "SUM('ORDER_DETAILS'[quantity])"
        tests:
          - not_null
      - name: YTD Revenue
        description: "CALCULATE([Total Revenue], DATESYTD('ORDER_DETAILS'[order_date]))"
        tests:
          - not_null
      - name: PY Revenue
        description: "CALCULATE([Total Revenue], SAMEPERIODLASTYEAR('ORDER_DETAILS'[order_date]))"
        tests:
          - not_null
      - name: YoY Growth
        description: "DIVIDE([YTD Revenue] - [PY Revenue], [PY Revenue], 0)"
        tests:
          - not_null
      - name: Fulfillment Rate
        description: "DIVIDE(
    CALCULATE([Total Orders], 'ORDER_DETAILS'[delivery_status] <> "Not Shipped"),
    [Total Orders], 
    0
)"
        tests:
          - not_null
      - name: New Customers
        description: "
CALCULATE(
    DISTINCTCOUNT('ORDER_DETAILS'[customer_id]),
    FILTER(
        'ORDER_DETAILS',
        'ORDER_DETAILS'[order_date] = 
        CALCULATE(
            MIN('ORDER_DETAILS'[order_date]),
            ALLEXCEPT('ORDER_DETAILS', 'ORDER_DETAILS'[customer_id])
        )
    )
)"
        tests:
          - not_null